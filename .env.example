DB_USER=chromebook_eol
DB_PASSWORD=change_me_strong_password
SECRET_KEY=change_me_32_char_random_secret_key

# Base64-encoded GCP service account JSON
# Generate with: [Convert]::ToBase64String([IO.File]::ReadAllBytes("service_account.json"))
SERVICE_ACCOUNT_B64=

# Sync schedule (24-hour UTC)
SYNC_HOUR=2
SYNC_MINUTE=0
